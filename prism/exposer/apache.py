from .. import log

def depends(app, args):
    log.fail('Apache not supported, yet!')

def create(app, args):
    log.fail('Apache not supported, yet!')

def destroy(app, args):
    log.fail('Apache not supported, yet!')

def restart(app, args):
    log.fail('Apache not supported, yet!')
